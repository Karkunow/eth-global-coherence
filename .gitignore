# Judges require all spec files, prompts, and planning artifacts in the repo,
# so .claude/skills/ (the Spec Kit workflow prompts) is deliberately tracked.
# Only machine-local state is ignored.
.claude/settings.local.json
.env
*.pyc
__pycache__/
.venv/
venv/
*.egg-info/
.DS_Store
.vscode/
.idea/
*.swp
*.swo
